<h1>{{ title }}</h1>
<table class="table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
      </tr>
    </thead>
    <tbody>
    {% for item in items %}
      <tr>
        <td>{{item.title}}</td>
        <td>{{item.body}}</td>
      </tr>
     {% endfor %} 
    </tbody>
  </table>